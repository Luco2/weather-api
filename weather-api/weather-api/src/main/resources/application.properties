# Specify the database connection properties
spring.datasource.url=jdbc:postgresql://localhost:5432/weather_api_db
spring.datasource.username=postgres
spring.datasource.password=postgres1
spring.datasource.driver-class-name=org.postgresql.Driver

# Specify Hibernate properties (if using Hibernate)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Optionally, you can configure the server port (default is 8080)
server.port=8081

api.url=http://api.weatherapi.com/v1
api.key=84aa99ac3f924950a36191138232206
